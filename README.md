# TechStart - Лендинг

Современный лендинг для IT-компании, разработанный на стеке React + TypeScript + Effector + Webpack.

## 🚀 Быстрый старт

Чтобы запустить проект локально из GitHub:

1. Клонируйте репозиторий:
   ```bash
   git clone <URL-репозитория>
   cd techstart
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите в режиме разработки:
   ```bash
   npm start
   ```

Приложение откроется в браузере по адресу: http://localhost:3000

## 🚀 Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Effector** - state manager для управления состоянием приложения
- **Webpack 5** - сборщик модулей
- **CSS Modules** - модульные стили с локальной областью видимости
- **CSS Variables** - кастомные свойства для темизации

## 📁 Структура проекта

```
techstart/
├── public/
│   └── index.html          # HTML шаблон
├── src/
│   ├── components/         # React компоненты
│   │   ├── Navbar/         # Навигация
│   │   ├── Hero/           # Главный экран
│   │   ├── About/          # О компании
│   │   ├── Services/       # Услуги
│   │   ├── Portfolio/      # Портфолио
│   │   ├── Testimonials/   # Отзывы
│   │   ├── Contact/        # Форма контактов
│   │   └── Footer/         # Подвал
│   ├── hooks/              # Кастомные хуки
│   ├── store/              # Effector store
│   ├── styles/             # Глобальные стили
│   ├── types/              # TypeScript типы
│   ├── App.tsx             # Главный компонент
│   └── index.tsx           # Точка входа
├── package.json
├── tsconfig.json
├── webpack.config.js
└── README.md
```

## 🛠 Установка и запуск

### Требования

- Node.js >= 16.0.0
- npm >= 8.0.0

### Установка зависимостей

```bash
cd techstart
npm install
```

### Режим разработки

```bash
npm start
# или
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Проверка типов

```bash
npm run type-check
```

## ✨ Особенности

### Адаптивность
- Mobile-first подход
- Брейкпоинты: 575px, 767px, 991px, 1200px
- Адаптивная типографика с `clamp()`
- Мобильное меню с анимацией

### Анимации
- Scroll-triggered анимации через Intersection Observer
- CSS keyframe анимации
- Плавные переходы (transitions)
- Hover эффекты на карточках
- Анимированный фон в Hero секции

### State Management (Effector)
- Управление состоянием меню
- Отслеживание активной секции
- Обработка формы контактов
- Эффекты для асинхронных операций

### Производительность
- Code splitting через Webpack
- CSS Modules для изоляции стилей
- Оптимизированные анимации (transform, opacity)
- Lazy loading изображений

## 📱 Секции

1. **Hero** - Главный экран с градиентным фоном и статистикой
2. **About** - Преимущества компании (3 карточки)
3. **Services** - Услуги с ценами (4 карточки)
4. **Portfolio** - Галерея проектов (6 проектов)
5. **Testimonials** - Отзывы клиентов (3 отзыва)
6. **Contact** - Форма обратной связи
7. **Footer** - Контакты и навигация

## 🎨 Кастомизация

### CSS Variables

Основные переменные находятся в `src/styles/variables.css`:

```css
:root {
  --color-primary: #6366f1;
  --color-secondary: #764ba2;
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  /* ... */
}
```

### Добавление новой секции

1. Создайте папку в `src/components/NewSection/`
2. Добавьте файлы: `NewSection.tsx`, `NewSection.module.css`, `index.ts`
3. Экспортируйте компонент в `src/components/index.ts`
4. Добавьте компонент в `App.tsx`

## 📄 Лицензия

MIT
